# Node.js(express), mongo, Taskapp.

- Node.js(express) - mongo(mongoose)
- eslint(airbnb, prettier)
- jest

### Usage

```
mongod --config /usr/local/etc/mongod.conf
npm run start
localhost:3000
```
